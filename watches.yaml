---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: thedatabase.me
  kind: Nextcloud
  role: nextcloud
  finalizer:
    name: nextcloud.thedatabase.me/finalizer
    vars:
      cleanup: true
#+kubebuilder:scaffold:watch
